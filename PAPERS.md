# 논문은 어떻게 다루나

> **"Paper GPT는 어디 있나요?"** 에 대한 답이 이 문서입니다.
> 결론부터: **별도의 Paper GPT를 만들지 않습니다.** 논문은 같은 러너 GPT의
> **정독 모드**로 다룹니다. 왜 그렇게 했는지와, 논문이 들어오는 4가지 길을 아래에 적었습니다.

---

## 왜 GPT를 하나로 두는가 (결정 기록)

논문용 GPT를 따로 두자는 제안이 있었습니다. 타당한 이유가 있었고요 — 논문을 읽는
방식은 경력에 따라 다르고(학부연구생 ↔ 석사 ↔ 박사), 논문을 실제로 안 읽고
답하면 할루시네이션이 납니다.

그런데 GPT를 나누면 **기록이 갈라집니다.** 이 시스템의 핵심 자산은 대화가 아니라
`STATUS.md` · `mastery.md` · `concepts.json`으로 쌓이는 **하나의 상태**입니다.
논문 세션에서 배운 개념이 별도 GPT에 갇히면, 개념 지도가 두 조각으로 쪼개지고
**막힌 길목 계산이 틀립니다**(막힌 길목은 전체 그래프에서만 나옵니다).

그래서 이렇게 갈랐습니다:

| 나누는 것 | 안 나누는 것 |
|---|---|
| **세션 대본** — 정독 세션은 기본 세션과 다른 순서로 진행된다 | **GPT · 저장소 · 개념 지도 · 이해도 원장** |
| **읽는 방식** — 경력 layer마다 다르게(아래) | 쓰기 계약(`[학습]`/`[자료]` Issue → CI) |

> 즉 "Paper GPT"에 해당하는 것은 **모드**이지 **앱**이 아닙니다. GPT를 새로 만들 필요가 없습니다.

## 읽는 방식은 경력마다 다릅니다

[`/start`](https://topdown-web.vercel.app/start) 위저드의 **논문 읽기 경험** 선택이
GPT 지침의 `# 이 학습자` 블록에 그대로 박힙니다:

| layer | 읽는 방식 |
|---|---|
| **학부연구생** | abstract → introduction → method → discussion **전체 정독**. 낯선 용어마다 멈춰 확인 |
| **석사** | method·figure 먼저, 주장-증거 연결을 비판적으로. 전체 정독은 핵심 논문만 |
| **박사 이상** | 기여·한계 중심으로 빠르게 위치 파악, 필요한 디테일만 |

어느 layer든 **완료 조건은 "읽었다"가 아니라 "스스로 설명할 수 있다"** 입니다.

---

## 논문이 들어오는 4가지 길

### ① 시작 논문 5편 — 위저드가 준다 (세팅 때 한 번)

[`/start`](https://topdown-web.vercel.app/start)의 **논문** 유형에서 아래 중 아무거나 넣으면
학술 DB(OpenAlex)에서 찾아 `STATUS.md`의 "읽을 논문" 목록과 부트 프롬프트에 얹어 줍니다:

- **교수님·연구자 영문 이름** (예: `Hoi-Jun Yoo`) → 동명이인 후보를 보여주고 고르면 대표 5편
- **논문 제목** (예: `Attention Is All You Need`)
- **DOI** (예: `10.1038/nature14539`)
- **arXiv 링크** (예: `https://arxiv.org/abs/1706.03762`)

> ⚠️ 안 되는 입력: **한글 이름**(`유회준`)과 **랩 홈페이지 URL**(`ssl.kaist.ac.kr`).
> 학술 DB는 영문 표기로만 찾고, 홈페이지는 아직 못 읽습니다. 위저드가 그렇게 알려 줍니다.
> 랩 링크는 "홈페이지·자료 링크" 칸에 적으세요 — **첫 세션에서 GPT가 직접 봅니다.**

### ② 매주 새 논문 자동 수집 — `topics.yaml`

`topics.yaml`에 주제를 적어 두면 **매주 월요일 아침** 내 repo의 자동화가 돌며
새 논문을 `papers/inbox.md`에 모으고 Issue로 알려줍니다. 서버도 비용도 없습니다.

**중요**: 템플릿의 기본값은 `topics: []`(비어 있음)이라 **켜기 전까지는 계속 0편**입니다.
위저드의 **④ 버튼**이 `topics.yaml`을 만들어 주니 그걸 커밋하면 켜집니다.
직접 적어도 됩니다 — **query는 영어로** 쓰세요(논문 검색은 영어가 압도적으로 잘 걸립니다).

```yaml
topics:
  - id: my-lab
    label: "우리 연구실 주제"
    query: "processing in memory neural network accelerator"
```

세션에서: `이번 주 새 논문 같이 보자` → 러너가 인박스에서 **오늘 목표와 가장 가까운 1편**만 고릅니다.

### ③ 세션 중에 찾기 — 그냥 말하면 된다

```
이 주제 논문 찾아줘
이 링크 같이 읽자
```

GPT의 **웹 검색**으로 찾습니다(4단계에서 켠 기능).

> ⚠️ **여기가 가장 틀리기 쉬운 지점입니다.** 웹 검색으로 찾은 논문은 제목·저자·연도가
> 섞이거나 없는 논문이 나올 수 있습니다. 그래서 러너는 **찾은 논문 5편을 먼저 보여주고
> "이게 맞아?"** 를 묻게 되어 있습니다. **틀리면 바로 말해 주세요.**
> 확실한 근거가 필요하면 ①의 DOI·arXiv 링크 경로를 쓰세요 — 그쪽은 학술 DB 조회라 추측이 없습니다.

### ④ 논문 한 편 깊이 읽기 — 정독 세션 (PDF 첨부)

```
(논문 PDF를 대화에 첨부하고) 이 논문 정독하자 — 절 단위로.
```

정독 세션은 기본 세션과 **다른 대본**으로 갑니다:

1. 읽기 전 — *"이 절에서 뭐가 나올 것 같아?"* 예측
2. 읽은 뒤 — **내가** 자기 말로 재구성 (GPT가 요약해 주지 않습니다)
3. 낯선 용어·수식은 그 자리에서 멈춰 내가 설명해 보기
4. 연결 — *"이 절이 논문의 주장에 왜 필요한가?"*

**한 세션에 논문 전체를 끝내지 않습니다.** 15~60분에서 끊고 다음 절은 다음 세션 —
어디까지 왔는지는 `STATUS.md`에 남습니다.

> ⚠️ **PDF는 반드시 대화에 첨부해야 합니다.** GPT가 내 저장소를 읽는 통로(Action)는
> **텍스트만** 읽습니다 — 저장소에 PDF를 올려 두어도 GPT는 못 읽습니다.
> 그래서 정독이 끝나면 그 논문의 **요약·개념 지도·빈칸 문제**가 `[자료]` Issue로 증류되어
> `materials/`에 남습니다. 다음 세션부터는 PDF 없이 그걸 끌어옵니다.
> **원문 PDF는 저장소에 넣지 않습니다**(저작권·용량).

---

## 정독과 학습은 다른 세션입니다

같은 GPT지만 하루에 섞지 마세요.

| | 언제 | 시작 문구 |
|---|---|---|
| **정독 세션** | 논문 한 편을 깊이 파는 날 | `이 논문 정독하자 — 절 단위로.` |
| **기본 세션** | 개념을 굳히는 날 | `STATUS.md 읽고 오늘 세션 시작하자.` |

정독으로 얻은 개념도 결국 같은 `mastery.md`에 쌓입니다 — **`설명가능`은 읽었을 때가
아니라 반박을 통과했을 때 올라갑니다.**

---

관련 문서: [SETUP.md](./SETUP.md) (세팅) · [GUIDE.md](./GUIDE.md) (매일 쓰는 법) · [papers/](./papers/) (인박스)
