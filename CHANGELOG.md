# 바뀐 것들

> 템플릿이 고쳐지면 여기에 **무엇이·왜** 바뀌었는지 적습니다.
> `template-sync` 워크플로가 매주 월요일에 확인해 PR을 열고, 이 파일의 최근 항목을
> 그 PR 본문에 붙입니다 — 그래서 무엇을 받는지 모른 채 머지하지 않아도 됩니다.
>
> ⚠️ 표시는 **머지만으로 끝나지 않는 것**입니다. GPT 지침을 다시 붙여넣어야 합니다.

---

## ⚠️ 2026-08-15 — 세션이 **몇 번 주고받았는지**를 셉니다 (지침 재붙여넣기 필요)

세션을 마감할 때 러너가 `save_session`에 `turns` 값을 함께 보냅니다. 그 세션에서 주고받은
횟수이고, 서버가 노트에 적어 줍니다.

**왜 이걸 세나**: 나중에 이 학습 러너를 ChatGPT 밖에서 직접 돌릴 때 드는 비용이 대화
길이에 **제곱으로** 붙습니다(매 턴 이제까지의 대화 전체를 다시 보내기 때문입니다).
지금은 "한 세션에 20번쯤 주고받겠지"라는 **추측** 위에 비용 계산이 서 있는데, 이 한 줄이
그것을 실측으로 바꿉니다. 세지 못한 세션은 **0이 아니라 빈칸**으로 둡니다.

⚠️ 이 변경은 지침 본문에 있으므로 **ChatGPT 지침을 다시 복사해 붙여넣어야 합니다**
(Topdown 활용 가이드 → 「바뀐 것들」).

### 함께 고친 것 — 세션이 0으로 잡히던 문제 **두 가지**

주간 집계가 세션을 놓치고 있었습니다. 두 경우 다 **열심히 한 사람일수록 안 한 것처럼**
보이는 방향이었습니다.

1. **과목 서랍을 쓰면** — 세션 노트는 트랙이 있으면 `daily/<과목>/`에 들어가는데,
   집계가 `daily/` 바로 아래만 셌습니다. 과목을 나눠 정리한 사람이 0으로 잡혔습니다.
2. **논문을 읽으면** — 논문 세션은 `papers/<논문>/sessions/`에 쌓이고 `daily/` 노트를
   만들지 않습니다. 논문만 읽은 사람은 세션이 아무리 많아도 0이었습니다.

둘 다 고쳤고, 다시는 그러지 않도록 검사를 붙였습니다.

## 2026-08-15 — 논문을 **다섯 칸**으로 나눠 봅니다 (지침 재붙여넣기 불필요)

지금까지 논문 이해도는 「미이해 / 부분 이해 / 기능적 이해 / 비판적 이해」 네 단계뿐이었는데,
이 네 칸은 **어디서 막혔는지를 뭉갭니다.** 방법은 아는데 실험 설계를 못 읽는 사람과 그
반대인 사람이 똑같이 「부분 이해」로 접혔습니다.

이제 논문이 흐르는 순서대로 다섯 칸을 따로 셉니다:

**문제 → 한계 → 방법 → 실험 → 결과**

칸은 **자기 말로 설명해서 통과했을 때만** 채워집니다. 러너가 설명해 준 것은 통과가
아닙니다. 여러 세션에 걸쳐 채우면 되고, **이전에 채운 칸은 자동으로 이어집니다** —
러너가 매번 다시 적지 않습니다(적으라고 하면 지어냅니다).

함께 바뀐 것:

- **판정 근거가 남습니다.** 이해도를 올리거나 칸을 채우면, 그 판정의 근거가 된 **여러분의
  말이 그대로** 세션 기록에 인용됩니다. 나중에 "이게 정말 통과였나"를 되짚을 수 있습니다.
- **완독의 뜻이 엄격해졌습니다.** 미뤄 둔 선수지식(Parking Lot)이 절반 넘게 남아 있으면
  러너가 "다 읽었다"고 말하지 않고 몇 개가 남았는지 말합니다. 본문을 끝까지 넘긴 것과
  그 논문을 읽은 것은 다릅니다.

이 변경은 **모드 대본 파일**에서 일어나므로 GPT 지침을 다시 붙여넣을 필요가 없습니다.

## 2026-08-14 (2) — 렉처노트·교재에 그은 「막혔다」를 러너가 봅니다

앱의 읽기 화면에서 파킹랏·주석을 그으면 저장소에 쌓입니다. 논문(`papers/`)은 러너가
세션 시작에 그것을 읽는데, **자료(`materials/` — 렉처노트·교재)는 파일만 만들어지고
러너가 못 보고 있었습니다.** 경로를 알려 줄 곳이 없었기 때문입니다.

이제 `get_state`의 `materials` 항목마다 경로가 함께 옵니다:

- `parkingLot` — 읽다가 막혀서 미뤄 둔 것
- `annotations` — 원문에 남긴 인용과 코멘트

그리고 과목 모드 대본이 **세션을 열 때 그것을 읽도록** 바뀌었습니다. 「첫 응답의 모양」
①의 *"어디서 막혔는지"*가 거기서 나옵니다. 막힌 것이 오늘 절과 닿아 있으면 **그것을 오늘
목표로 올립니다** — 막힌 채로 다음 절로 넘어가면 같은 곳에서 또 막히니까요.

**도구는 여전히 넷입니다**(`get_state` · `read_doc` · `get_paper` · `save_session`).
`get_material`을 새로 만들면 커넥터 안내의 「도구 4개가 보이면 끝」이 거짓이 되고, 무엇보다
필요가 없었습니다 — 자료 목록을 만들 때 이미 그 폴더를 훑고 있어서 파킹랏이 있는지
**알면서 안 알려주고 있던** 것뿐입니다. 그래서 호출도 하나도 안 늘었습니다.

**지침을 다시 붙여넣지 않아도 됩니다.**

---

## 2026-08-14 — 세션이 **수업처럼** 열립니다 (복습 → 오늘 위치·목표 → 짧은 정리 → 질문)

지금까지 「오늘 세션 시작」·「이 논문으로 읽어보기」·「세션 이어가기」를 누르면 러너가
**질문 하나를 던지고 끝냈습니다.** 새 수업을 «그냥 이거에 대해 생각해봐»로 시작하는 셈이라
지난 시간과 이어지지도, 오늘 무엇을 끝내야 하는지도 알 수 없었습니다.

원인은 대본이 아니라 **첫 문장**이었습니다. 지침에는 이미 5단계가 있고 논문 모드에는
「지난 세션의 이해를 한 문단으로 복원」까지 있었는데, 앱이 넘기는 프롬프트가 전부
*"먼저 나에게 첫 질문 하나만 해줘"*로 끝나서 그 앞을 잘라먹고 있었습니다.

이제 세션을 **여는** 세 자리에서 첫 응답이 다섯 박자로 옵니다:

1. **지난 자리** — 지난번 어디까지 했고 무엇을 설명하게 됐는지, 어디서 막혔는지
2. **오늘 자리** — 그래서 오늘은 어디로 넘어가는지
3. **오늘 목표** — "오늘 끝나면 ___를 스스로 설명할 수 있게 된다"(= 완료 조건)
4. **짧은 정리** — 오늘 범위의 정의와 핵심 관계 5~8줄
5. **첫 질문 하나** — 방금 정리한 것을 자기 말로 다시 세우게 하는 질문

**4번이 새 규칙입니다.** 예전 대본은 *"러너가 먼저 요약하지 않는다"*였는데, 시작이 너무
막막하다는 판단으로 **말하는 순서를 완화**했습니다. 완화된 것은 순서뿐입니다:

- 정리에는 **예산**이 있습니다 — 정의와 핵심 관계만, 유도·계산·예시는 학습자 몫입니다.
- ⚠️ **러너의 정리를 읽은 것은 통과가 아닙니다.** 「설명가능」 승급은 여전히 학습자가
  자기 말로 설명하고 러너의 반박을 통과했을 때만입니다.
- 세션 노트의 「오늘 직접 학습한 지식」에도 여전히 **학습자가 설명한 말만** 들어갑니다.

읽다가 형광펜으로 문장 하나를 물어보는 자리는 **그대로입니다** — 이미 수업 중인데 복습부터
다시 하면 흐름이 끊깁니다.

**지침을 다시 붙여넣지 않아도 됩니다.** 앱과 이 저장소의 모드 대본
(`runner/paper-mode.md` · `runner/subject-mode.md`)만 바뀌었고, 대본은 러너가 세션 중에
읽습니다.

---

## 2026-08-10 — ⚠️ 러너가 **원문을 읽고** 묻고 교정합니다 (자료·논문 모두)

지금까지 러너가 자료에 대해 아는 것은 **증류본(요약)** 이었습니다. 논문은 더했습니다 —
`papers/<slug>/`에 원문이 없고 `paper.md`는 정제본이라 **학습자가 이미 설명해낸 것만**
담기므로, 러너가 아는 논문은 학습자가 아는 만큼이었습니다. 검사자가 피검사자의 답안으로
문제를 내는 꼴이라, 학습자가 놓친 것은 **물어볼 수조차 없었습니다.**

요약으로는 좋은 질문을 낼 수 없고 교정은 아예 불가능합니다. 그래서 원문을 읽게 했습니다.

- **원문이 모든 자료에 남습니다.** 논문도 `papers/<slug>/source.md`로 보존됩니다
  (앱에서 올릴 때 종류를 「논문」으로 고르면 그 논문 폴더로 갑니다).
- **절 단위로 읽습니다.** `read_doc`에 `section`이 생겼습니다 — 번호만(`3.2.1`)·제목만·
  둘 다 어느 쪽도 받고, 틀리면 실제 절 목록이 돌아옵니다. 논문 한 편을 통째로 받지 않습니다.
- **어느 절을 열지는 증류본의 `## 원문 목차`가 알려줍니다.** CI가 자료를 가를 때 원문에서
  긁어 적습니다(LLM 안 씁니다).
- **자료 진도가 생겼습니다** — `materials/READING_STATUS.md`. 세션 노트에
  `## 자료 진도 갱신` 절을 남기면 CI가 옮겨 적습니다(논문의 READING_STATUS와 같은 방식).
  이게 없으면 다음 세션이 "어디부터"를 짐작하고, 짐작이 틀리면 이미 한 절을 또 합니다.
- **읽었는지 서버가 확인합니다.** `save_session`에 `sourceAnchors`(원문에 **글자 그대로**
  있는 짧은 문자열)를 넘기면 서버가 파일을 열어 정말 있는지 봅니다. 원문을 안 읽었으면
  댈 수 없으므로 지어낼 수 없습니다. **지금은 경고까지만** 하고 기록은 그대로 남습니다.

지침 자체는 안 바뀌었고 모드 대본만 바뀌었으므로 → **재붙여넣기 불필요.**

## 2026-08-09 — 레지스트리를 쓰더라도 새 개념 지도가 그래프에 들어갑니다

`knowledge/concepts.yaml`을 채워 레지스트리 모드로 쓰면, 빌더가 그것만 읽고
`daily/`·`materials/`의 `## 개념 지도`는 **아예 안 봤습니다.** 자료를 올려 증류까지 됐는데
그래프의 노드가 하나도 안 느는 상태입니다 — 조용해서, 사람은 증류가 실패한 줄 알고 같은
자료를 다시 올리게 됩니다.

이제 **레지스트리가 이기되, 거기 없는 개념·간선은 노트에서 받습니다.**

- 간선 — 레지스트리에 없는 쌍만 더합니다.
- 개념 — 레지스트리 밖 라벨만 노드로 세우고, **판정은 여전히 원장(`mastery.md`)**에서 옵니다.
- 분야 — 레지스트리 분야가 이깁니다. 노트 분야는 레지스트리 밖 개념에만 붙습니다.
- 근거 — 레지스트리 개념의 근거는 **큐레이션된 앵커뿐**입니다(노트에서 더 긁지 않습니다).

빌드가 "레지스트리 밖에서 개념 N개 · 선수관계 M개를 노트에서 받았다"고 알려 주므로, 정리해
큐레이션에 올릴지는 그때 정하면 됩니다. 이름이 겹쳐 보이면 `concepts-overrides.yaml`의
`renamed`로 합칩니다. 레지스트리를 안 쓰는 저장소는 아무것도 달라지지 않습니다.

## 2026-08-09 — 앱에서 올린 자료가 **자동으로 증류**되고 원문도 남습니다

앞 항목(라벨 정직성)이 드러낸 문제의 나머지 절반입니다. 라벨이 정직해지자 앱에서 올린
자료가 죄다 `raw`로 찍혔습니다 — 증류가 **실제로 없었기 때문**입니다.

이제 Topdown 앱이 PDF를 올리는 시점에 요약·`## 개념 지도`·빈칸 문제를 만들어 Issue 초안에
함께 넣습니다. 그래서 CI가 파일을 둘로 나눕니다:

- `materials/<slug>/distilled.md` — 요약·개념 지도·빈칸 문제 (`tags: [material, distilled]`)
- `materials/<slug>/source.md` — **원문 그대로** (`tags: [material, source]`)

원문을 남기는 이유는 인용 근거입니다. 러너는 평소 증류본을 읽고, 원문을 인용해야 할 때만
source를 읽습니다. `## 개념 지도`가 증류본에만 있으므로 그래프에 같은 간선이 두 번 들어가지
않습니다. 전에 단일 파일로 저장돼 있던 같은 자료는 폴더형으로 올라갈 때 지워집니다
(남겨 두면 개념 지도가 두 번 읽힙니다).

**CI는 여전히 LLM을 부르지 않습니다.** 증류는 앱에서 하고 CI는 가르기만 합니다 — 이
저장소의 "CI는 LLM 토큰 0" 원칙이 그대로입니다. 세션에서 러너가 직접 증류하는 기존 경로도
그대로이고, 그쪽은 지금처럼 `materials/<slug>.md` 한 파일로 남습니다.

증류가 실패하면(무료 할당 소진 등) 원문만 `raw`로 저장됩니다 — 없는 것을 있다고 하지
않습니다. 지침 변경 없음 → **재붙여넣기 불필요.**

## 2026-08-09 — `[자료]` 라벨이 사실을 말합니다 (증류 안 됐으면 `raw`)

`[자료]` Issue로 저장되는 파일은 frontmatter에 **조건 없이** `tags: [material, distilled]`와
`source: "자료 증류 → …(원문 PDF는 저장하지 않음)"`를 적고 있었습니다. 그런데 증류는
**세션에서 러너가 하는 것**이라, 앱에서 PDF를 파싱해 초안만 만들고 세션 없이 닫으면
본문 전체가 원문인 채로 `distilled` 딱지가 붙습니다(실측: 102KB 전부 원문, `## ` 헤딩은
`러너에게`·`원문` 둘뿐 — 요약도 개념 지도도 문제 은행도 없음).

조용한 거짓말이라 **"이 파일은 증류본이니 원문이 아니다"라는 저작권 판단까지 오도**합니다.

이제 본문을 보고 정합니다 — `## 개념 지도`가 있으면 `distilled`, 없으면
`tags: [material, raw]` / `source: "원문 텍스트 (증류 없음)"`입니다. `## 개념 지도`를
기준으로 삼은 이유는 셋 중 **파이프라인이 실제로 먹는 것**이기 때문입니다(빌더가 거기서만
선수관계를 읽어 갑니다). CI 보고도 같이 고쳤습니다 — 증류가 없으면 없다고 말하고, 세션에서
증류하는 법을 알려 줍니다. **CI가 대신 증류하지는 않습니다**(이 저장소의 CI는 LLM 토큰 0이
원칙입니다).

지침 변경 없음 → **재붙여넣기 불필요.**

## 2026-08-09 — 노트가 쌓였는데 그래프만 비어 있을 때 (개념 레지스트리)

선수관계는 세션이 `## 개념 지도`를 남긴 만큼만 쌓입니다. 그래서 **그 관례가 생기기 전에 쓴
노트로 배운 개념은 영원히 간선 0**입니다 — 실측으로 개념 44개에 선수관계 0·분야 0이었습니다.
이 시스템의 핵심인 **막힌 길목**은 화살표가 있어야 계산되므로, 그 상태에서는 아무리 공부해도
"무엇이 무엇을 막고 있는지"가 나오지 않습니다. 조용한 실패라 알아채기 어렵습니다.

이제 `knowledge/concepts.yaml`에 개념을 한 번 적어 그 공백을 메울 수 있습니다(레지스트리).
개념의 정체(도메인·선수관계·외부 문서)만 적고, **본문은 적지 않습니다** — 원문에 그대로 있는
짧은 문자열을 앵커로 달면 빌드가 그 자리의 원문 블록을 통째로 꺼냅니다(요약본을 만들지
않습니다). 같은 저장소에서 개념 44·간선 0 → **개념 72·간선 75·분야 10·원문 근거 161조각**이
됐습니다.

- **판정은 여전히 원장(`mastery.md`)이 SSOT입니다.** 레지스트리의 `mastery:`가 원장의 행
  이름을 가리키고, `scripts/check_concept_ledger.py`가 둘이 갈렸는지 검사합니다.
- **레지스트리는 별도 빌드 경로가 아닙니다.** 네 재료(개념·선수관계·분야·근거)로 번역돼
  기존 파이프라인에 합류하므로, 지도 손보기(`concepts-overrides.yaml`)·분야
  정규화(`subjects.yaml`)·개념/항목 분리가 모드와 무관하게 똑같이 돕니다.
- **안 쓰면 아무것도 안 바뀝니다.** 함께 오는 `knowledge/concepts.yaml`은 **빈 견본**이고,
  비어 있는 동안은 지금까지처럼 `mastery.md` + daily로 그래프를 짓습니다.
- 채워 둔 레지스트리는 동기화가 **덮지 않습니다**(NEVER 목록 + create-only 씨앗).

지침 변경 없음 → **재붙여넣기 불필요.**

## 2026-08-05 — `subjects.yaml`·`tracks.yaml`이 옛 저장소에 영영 안 오던 것

두 파일은 **학습자의 것**이라 동기화가 덮어쓰지 않습니다(NEVER 목록). 그런데 그 탓에
**없을 때 만들어 주지도 않았습니다.** 그 파일들이 생기기 전에 만들어진 저장소는 아무리
동기화해도 계속 없는 상태로 남았습니다.

결과가 조용했습니다 — 러너는 세션 시작에 `subjects.yaml`을 읽으려다 404를 받고 분야
이름을 **매번 새로 지었습니다.** 파일럿에서 "회로 등가화 6 · 전자회로 5 · 전자회로 기초 4"가
각각 다른 분야가 된 원인입니다(셋 다 같은 전자회로입니다).

이제 **없으면 만들고, 있으면 절대 손대지 않습니다**(create-only). NEVER의 뜻은
"덮어쓰지 마라"이지 "만들지도 마라"가 아닙니다. 이미 분야를 묶어 둔 저장소는
그대로입니다 — 테스트로 고정했습니다.

지침 변경 없음 → **재붙여넣기 불필요.** 다음 동기화(매일 09:20 KST) 때 자동으로 옵니다.
## 2026-08-05 — 읽기는 되는데 Issue만 안 만들어질 때

파일럿에서 나온 증상입니다. `STATUS.md`도 모드 대본도 잘 읽어 오는데 **기록만 실패**합니다.

증상 자체가 답입니다 — **토큰은 살아 있고 저장소도 맞는데 `Issues` 권한만 모자란 것**입니다.
읽기는 `Contents`, 기록은 `Issues`로 권한이 갈리기 때문입니다.
Fine-grained 토큰의 **Issues를 `Read and write`** 로 바꾸면 됩니다(`Read-only`면 이 증상).

`SETUP.md`의 「막히는 지점」에 이 진단과, **러너가 출력한 노트를 살리는 법**(제목만
`[학습] YYYY-MM-DD <slug> — <제목>`으로 맞춰 직접 Issue에 붙여넣으면 CI가 똑같이 돕니다)을
넣었습니다. 지침 변경은 없으므로 **재붙여넣기가 필요 없습니다.**


## 2026-08-05 — ⚠️ 개념 지도가 **커리큘럼**이 됩니다 (학습 로그가 아니라)

유지훈의 제안을 반영했습니다. 지금까지 `## 개념 지도`에는 **오늘 다룬 개념만** 적혀서
지도가 세션마다 조각조각 자랐습니다. 그래서 *"테브난이 대체 대학 과목 어디에 있는
거지"*에 답할 수 없었습니다.

앞으로는 **목표를 설명하기 위해 필요한 선수관계 그래프 전체**를 먼저 그립니다:

- 아직 안 배운 개념도 포함합니다 → `미학습` 노드로 서고, 그게 **앞으로 배울 길목**입니다.
- 세션에서는 개념을 늘리기보다 **상태만 갱신**합니다(`암기` → `설명가능`).
- 목표가 바뀌면 다시 그리되 기존 개념 이름은 재사용합니다(바꾸면 증거 링크가 끊깁니다).

밀도 기준도 함께 고쳤습니다 — "10~30개"는 **과목 하나당**입니다. 커리큘럼 전체는 여러
과목을 가로지르므로 40~60개가 자연스럽습니다.

**대본을 `runner/concept-map.md`로 내렸습니다.** 지침 박스가 7,000자로 꽉 차서, 개념의
단위·간선·분야 규칙을 저장소 파일로 옮겼습니다(논문·시험 모드와 같은 구조). 앞으로 이
규칙은 고쳐도 지침을 다시 붙여넣을 필요가 없습니다.

> ⚠️ **이번 한 번은 지침을 다시 붙여넣어야 합니다** — 대본을 읽으라는 진입점이 지침에
> 새로 생겼기 때문입니다. Topdown 활용 가이드 → 「바뀐 것들」의 「지침 복사」를 쓰세요.


## 2026-08-05 — ⚠️ 러너가 액션을 못 부르고 웹 검색·파이썬으로 새던 것

세션에서 이런 일이 났습니다:

- `웹사이트 1개 검색 중` — 저장소를 읽는 대신 웹 검색을 했습니다.
- `from api_github_com__jit_plugin import readFile` → `ModuleNotFoundError` — 액션을
  **파이썬(코드 인터프리터)에서 import** 하려 했습니다. 그 샌드박스에는 액션도 네트워크도
  없어서 절대 성공하지 않습니다.
- 그리고 개념 지도를 다 만들어 놓고 *"저장소 파일을 수정할 권한이 없습니다"*라며
  **출력만 하고 끝냈습니다.** 파일 API가 없는 것이지 Issue는 늘 쓸 수 있는데도요.

지침에 셋을 못박았습니다 — ① 액션은 직접 호출하고 코드로 부르지 않는다 ② 못 부르면
웹 검색으로 갈아타지 말고 "저장소를 못 읽었다"고 말한다(검색 결과는 이 학습자의 기록이
아닙니다) ③ "지도 적어줘"도 세션이니 Issue를 열어 적는다.

> ⚠️ **GPT 편집 → Instructions를 다시 붙여넣어야 합니다.** Topdown 활용 가이드 →
> 「바뀐 것들」의 「지침 복사」를 쓰면 내 저장소가 적힌 채로 복사됩니다.
>
> 그래도 러너가 "호출할 수 없다"고 하면 지침이 아니라 **액션이 안 붙은 것**입니다:
> GPT 편집 → Actions에 `readFile`·`createNote`가 보이는지 확인하고, 없으면 스키마를
> 다시 붙여넣은 뒤 **Authentication을 다시 지정하세요**(스키마를 저장하면 인증이
> 풀리는 경우가 있습니다 — Bearer + 내 GitHub 토큰).


## 2026-08-04 — ⚠️ 러너가 저장소를 **전혀 못 읽고 있었습니다** (Action 스키마 수정)

러너 GPT가 저장소를 읽지 않고 웹 검색만 했습니다. 스키마가 원인입니다.

읽기 경로가 `/repos/{owner}/{repo}/contents/{path}` **하나**였는데, 경로 파라미터는
클라이언트가 **퍼센트 인코딩**합니다 — `runner/paper-mode.md`가 `runner%2Fpaper-mode.md`로
나가고 GitHub은 404를 줍니다. 즉 **하위 폴더 파일은 하나도 못 읽었습니다**(루트의
`STATUS.md`만 우연히 동작). 404가 반복되면 GPT는 액션을 포기하고 웹 검색으로 넘어갑니다.

폴더를 URL에 고정하고 파라미터는 **슬래시 없는 이름만** 받도록 쪼갰습니다:

| 액션 | 무엇을 읽나 |
|---|---|
| `readFile` | 루트 (`STATUS.md`·`mastery.md`·`drills.md`·`topics.yaml`) |
| `readRunnerFile` | 모드 대본 (`paper-mode.md`·`exam-mode.md`·`topics.md`) |
| `readPapersFile` · `readPaperFile` | `papers/` 공용 · 논문 한 편의 파일 |
| `readDailyFile` · `readMaterialFile` | 지난 세션 노트 · 증류된 자료 |
| `listPapers` | 논문 폴더 목록 (slug를 지어내지 않게) |

> ⚠️ **GPT 편집 → Actions → Schema를 다시 붙여넣어야 합니다.** 붙여넣은 뒤 확인:
> *"runner/paper-mode.md 읽어봐"* → 평문이 오면 성공, 웹 검색을 하면 실패입니다.

`scripts/test_runner_instructions.py`가 이제 스키마도 검사합니다 — 슬래시를 품는 경로
파라미터가 있거나, 지침이 부르는 액션이 스키마에 없으면 실패합니다.

## 2026-08-04 — Parking Lot 항목을 세션으로 열면 **끝까지 이해시킵니다**

밀어 둘 때는 "최소 의미 한 줄"이지만, 그 항목을 오늘의 주제로 꺼낸 순간 규칙이 바뀝니다.
요약으로 끝내면 Parking Lot은 줄지 않는 목록이 되고 같은 단어를 세 번째로 다시 만납니다.

완료 조건이 둘입니다: ⓐ 자기 말로 설명하고 러너의 반박을 통과 ⓑ **그 논문의 원래 문장으로
돌아가** 이제 그 문단이 왜 그렇게 읽히는지 말하기. 항목만 알고 논문으로 못 돌아오면 배운
것이 아닙니다. 한 세션에 못 끝냈으면 **해소로 표시하지 않습니다** — 절반을 해소로 적는
순간 그 목록은 거짓말을 시작합니다.

## 2026-08-04 — 템플릿 업데이트가 **세션이 끝날 때** 따라옵니다 (전에는 최대 7일)

`template-sync`는 **매주 월요일**에만 돌았습니다. 그래서 템플릿을 고쳐도 각자 저장소는
최대 7일 뒤에야 받았고, 그 사이 새 기능은 *"왜 내 repo에선 안 되지"* 로만 보였습니다
(실제로 앱이 만든 `[자료]` Issue가 도착지 없이 사라졌습니다 — 그 저장소의 워크플로가
아직 `[자료]`를 모르는 옛 버전이었습니다). 사람이 Actions 탭에서 손으로 돌려야 하는
것은 자동화가 아닙니다.

- **세션이 끝나면 동기화가 깨어납니다.** `learning-note-ingest`가 노트를 접고 커밋한
  **뒤에** `template-sync`를 호출합니다. 그래서 **다음 세션은 언제나 최신으로 시작**합니다.
  (기록을 세션 시작에 걸었던 것과 같은 이유 — 관측 가능한 사건에 겁니다.)
- **cron은 주 1회 → 매일**로 바꿨습니다. 세션이 없는 기간의 그물입니다.
- 동기화가 실패해도 학습 기록은 영향받지 않습니다(세션 커밋 뒤에 돌고, 실패는 무시).

> ⚠️ **이 변경 자체는 각 저장소가 한 번 동기화를 받아야 적용됩니다**(닭-달걀). 지금
> 옛 버전인 저장소는 **Actions → template-sync → Run workflow**를 한 번만 눌러 주세요.
> 그 뒤로는 손댈 일이 없습니다.

## 2026-08-04 — 논문 주제가 **분야로 고정**됩니다 (`HBM` → human breast milk 사고)

`topics.yaml`에 `HBM`으로 주제를 잡았더니 high bandwidth memory가 아니라 **human breast
milk** 논문이 왔습니다. 검색어를 다듬어 고칠 문제가 아닙니다 — 자연어 검색은 **분야를
모르기 때문에**, 약어가 겹치는 모든 주제에서 같은 일이 납니다(`MI`: motor imagery /
myocardial infarction, `CNN`, `ADC`, `BCI`…). 배제어 목록으로 막으면 겪은 오검출만 막고
처음 보는 것은 못 막습니다.

**주제를 단어가 아니라 OpenAlex의 분야 id로 고정합니다.** 모든 논문에는
topic → subfield → field → domain 계층이 붙어 있어서, 주제에 분야를 걸면 다른 분야의
논문은 **애초에 후보에 들어오지 못합니다.**

분야 코드를 사람이 찾아 적을 일은 없습니다 — **씨앗 논문(`seed:`)** 의 DOI 하나면
CI가 그 논문의 분야를 읽어 필터로 씁니다. "이 논문과 같은 분야"가 사람이 실제로 갖고
있는 정보이고, 방금 읽은 논문이 그 주제의 가장 좋은 정의입니다.

- `topics.yaml`에 `seed` · `topics` · `field` · `exclude`가 생깁니다(전부 선택).
- 분야가 고정되지 않은 주제는 `papers/inbox.md` 맨 위에 **경고로 뜹니다** — 조용히
  엉뚱한 논문을 섞지 않습니다. 배제어로 거른 편수도 적힙니다.
- 세션에서 러너에게 *"이 주제 분야 고정해줘"* 라고 하면 씨앗 논문으로 잡아 줍니다.

## 2026-08-04 — 연구 주제를 러너가 잡아 줍니다 (`topics.yaml`을 손으로 안 씁니다)

`topics.yaml`을 사람이 손으로 쓰는 것이 병목이었습니다. YAML 문법이 깨지고, 영어 검색어를
짓기 번거롭고, 분야 고정은 아무도 하지 않습니다.

이제 러너가 **첫 세션에서 목표가 정해졌을 때**와 **논문 한 편을 다 읽었을 때** 주제를
제안하고, 승인받으면 `[설정] topics — …` Issue에 평문으로 넘깁니다. 파일은 CI가 씁니다
(학습 노트와 같은 방식 — 모델은 평문만, 파일은 CI. LLM 토큰 0, API 키 불필요).

- 적지 않은 주제는 **지워지지 않습니다**(id 단위 병합). 지우려면 `remove: true`.
- 파일 위쪽의 주석·설정(`mailto`·`window_days`)은 그대로 보존됩니다.
- 규칙은 `runner/topics.md`에 있습니다 — 러너가 제안 직전에 읽습니다.

## 2026-08-04 — 논문별 저장소: `meta.yaml` · Parking Lot · 아티팩트

논문 세션이 만들어 내는 것 중 세 가지가 받는 곳이 없어 세션 원문에만 묻혀 있었습니다.
묻히면 다음 세션의 러너가 못 찾고, 같은 선수지식을 매번 다시 미룹니다.

| 새 절 | 어디로 | 규칙 |
|---|---|---|
| `## 메타` | `papers/<slug>/meta.yaml` | 서지 + **이해 4단계**(미이해→비판적). 키 단위 병합 |
| `## Parking Lot` | `papers/<slug>/parking-lot.md` | 항목 단위 병합. `- [x]` 해소는 되돌아가지 않음 |
| `## 아티팩트` | `papers/<slug>/artifacts/` | `### 제목`마다 파일 하나 |

논문 읽기 대본 자체도 [`runner/paper-mode.md`](./runner/paper-mode.md)로 정리했습니다 —
맥락 복원 → 사용자가 먼저 설명 → **맞음/부정확/빠짐/오개념 4갈래 교정** → 확인 질문
게이트 → Parking Lot → 시각 아티팩트 → 기록.

## 2026-08-04 — ⚠️ 러너 지침이 8,000자 한도를 넘어 있었습니다 (잘려서 붙던 것)

ChatGPT Custom GPT의 **Instructions 칸 한도는 8,000자**입니다. 그런데 지침이 자라
**10,640자**가 되어 있었습니다. 넘으면 오류가 뜨는 게 아니라 **뒤쪽이 조용히 잘립니다** —
하필 잘리는 자리가 **쓰기 계약(본문 형식)** 이라, GPT가 노트를 엉뚱한 헤딩으로 써도
아무도 눈치채지 못하는 실패였습니다. 새로 GPT를 만든 사람은 지침 저장 자체가 막혔습니다.

**7,352자로 줄였습니다. 규칙은 하나도 빼지 않았습니다** — 뺀 것은 근거·역사·해설(예:
"예전 계약은 이랬고 왜 바뀌었나")이고, 그건 원래 대본이 아니라 설명서에 있을 것들입니다.
CI가 읽는 헤딩 이름·Issue 제목 규칙·액션 이름은 **한 글자도 바뀌지 않았습니다.**

- 다시 붙여넣어야 합니다: GPT 편집 → Instructions 를 **비우고** 갱신된
  `runner/instructions.md`의 회색 박스를 붙여넣습니다.
- 시험 트랙(토익 등)에서 `presets/toeic/runner-addendum.md`를 이어 붙이는 경우,
  본문에서 `# 논문·연구실 모드`와 `## 논문 정독 세션`을 빼고 붙입니다(시험 repo에서는
  쓰이지 않는 대본입니다). 그러면 합계 7,637자로 한도 안에 들어갑니다.
- 다시 자라지 않도록 `scripts/test_runner_instructions.py`가 길이와 계약 문자열을 잽니다.

## 2026-08-03 — 파일럿 집계 워크플로가 들어옵니다 (기본값: 꺼짐)

이번 동기화로 `.github/workflows/pilot-report.yml`이 저장소에 들어옵니다. **아무것도 하지
않습니다** — 저장소 변수 `PILOT_PARTICIPANT`를 직접 설정하지 않는 한 계산도, 전송도,
파일 생성도 없습니다. 파일럿에 참여하지 않으면 없는 것과 같습니다.

숨기지 않고 적는 이유는, 자기 저장소에 들어온 워크플로가 무엇을 할 수 있는지는 알고
있어야 하기 때문입니다.

**켰을 때 밖으로 나가는 것** (`scripts/pilot_rollup.py`):
세션 수·세션 간격·연속일·개념 수와 상태 분포·전이 시도와 판정·7일 재검증 판정·산출물
수·미해결 병목 수. **수치뿐입니다.**

**절대 나가지 않는 것:** 세션 노트 원문, 개념 이름, 목표와 트랙 텍스트, 저장소 이름,
사용자명, 파일 경로. 이건 문서상의 약속이 아니라 `scripts/test_pilot_rollup.py`가 매
실행마다 검사하며, 계약 밖의 항목이 하나라도 섞이면 전송이 실패합니다.

끄고 싶으면 `PILOT_PARTICIPANT` 변수를 지우면 됩니다. 그 뒤로는 아무것도 계산되지 않습니다.

## 2026-08-03 — ⚠️ 세션 기록에 「전이」와 「7일 재검증」이 생깁니다

지금까지 기록은 **"오늘 무엇을 이해했나"** 까지였습니다. 그런데 이해했다는 느낌은 다음 날
사라지고, 설명할 수 있다는 것과 **처음 보는 문제에 쓸 수 있다**는 것은 전혀 다릅니다.
그 둘을 재는 칸이 없어서, 잘 되고 있는지를 느낌으로만 알았습니다.

세션 노트에 절 두 개가 추가됩니다.

- **`## 전이 시도`** — 오늘 배운 것을 **처음 보는 다른 사례**에 적용해 본 결과.
  같은 문제를 숫자만 바꿔 다시 푼 것은 전이가 아닙니다.
- **`## 7일 재검증`** — 5~10일 전에 `설명가능`으로 올린 개념을 **자료 없이** 다시 설명해 본 결과.

**둘 다 실제로 했을 때만 씁니다.** 안 했으면 절을 통째로 뺍니다 — "없음"이라고 적지도
않습니다. 비어 있는 것 자체가 기록이고(그 주엔 전이를 안 했다는 뜻), 채우려고 지어내면
이 칸이 재는 것이 전부 거짓이 됩니다. 매 세션 해야 하는 것도 아닙니다.

그리고 코드·발표자료·재현 노트북처럼 **세션 밖에 남는 산출물**이 생기면 노트 첫 줄에
`artifact: <링크>`를 적습니다(`slug:`처럼 지시행입니다). 수집기가 이걸 frontmatter로
올려서, 나중에 "첫 결과물까지 얼마나 걸렸나"를 셀 수 있게 됩니다.

⚠️ **GPT 지침을 다시 붙여넣어야 합니다** — `runner/instructions.md`가 바뀌었습니다.
파일(수집기·템플릿)은 자동으로 따라옵니다.

## 2026-08-03 — paper-scan이 주제를 넣어도 0편만 나오던 문제

`topics.yaml`에 주제를 넣었는데 인박스가 계속 "0편"이었다면 이 버그입니다. 원인이 셋이고,
셋 다 결과가 "0편"으로 같아서 겉으로는 구별할 수 없었습니다.

- **초록 없는 논문을 전부 버렸습니다.** OpenAlex는 출판사 라이선스 때문에 상당수 논문의
  초록을 주지 않습니다 — 특히 신간이 그렇습니다. API가 25편을 정상으로 돌려줘도 선별에서
  전멸했습니다. 이제 버리지 않고 뒤로만 밀고, 인박스에 "초록이 공개돼 있지 않다"고 적습니다.
- **조회 실패가 성공으로 처리됐습니다.** 모든 주제의 조회가 죽어도 종료 코드가 0이라,
  인박스엔 "0편"이 적히고 실패 알림도 뜨지 않았습니다. 이제 전부 실패하면 Issue가 옵니다.
- **OpenAlex 공용 대기열에서 막혔습니다.** `topics.yaml`에 `mailto:` 한 줄(선택)을 넣으면
  여유 있는 대기열로 갑니다. 일시적 오류는 3번까지 다시 시도합니다.

받은 뒤 **Actions → paper-scan → Run workflow** 로 바로 확인할 수 있습니다.

## 2026-08-03 — 템플릿 업데이트를 자동으로 받습니다 (`template-sync`)

지금까지는 템플릿이 고쳐질 때마다 "이 파일들을 복사하세요"를 사람이 전해야 했습니다.
한 번이면 몰라도 계속 생기면 아무도 하지 않게 되고, 결국 각자 다른 버전을 쓰게 됩니다.

이제 **매주 월요일에 워크플로가 확인해 바로 적용하고, 무엇이 바뀌었는지 Issue로 알립니다.**
(PR로 받고 싶으면 저장소 변수 `TEMPLATE_SYNC_MODE=pr`.) 가져오는 것은 엔진
(`scripts/` · `runner/` · `presets/` · `templates/` · `.github/workflows/` · 문서)뿐이고,
`daily/` · `materials/` · `papers/` · `mastery.md` · `STATUS.md` · `drills.md` ·
`concepts.json` · `topics.yaml` · `README.md`는 **어느 경우에도 건드리지 않습니다.**

리뷰를 안전장치로 삼지 않습니다 — 쓰는 사람이 `build_concepts.py` diff를 읽고 판단할 수는
없으니, 읽지 않을 리뷰를 위해 마찰을 만들면 결국 아무도 안 받게 됩니다. 대신 **범위 제한**
(엔진만·기록은 절대 안 건드림·파일 삭제 없음)과 **적용 전 자동 검사**가 안전장치입니다.

**워크플로 파일은 일부러 얇게 만들었습니다.** GitHub 기본 토큰은 `.github/workflows/` 를
수정할 수 없어 그 파일들만은 자동 배포가 안 됩니다. 그래서 로직을 전부 `scripts/`로 내렸습니다
(`template_sync_ci.py` · `paper_scan_ci.py` · `ci_github.py` · `ci_notify_failure.py`).
워크플로에는 "체크아웃하고 스크립트를 부른다"만 남아서, **앞으로 고치는 것은 전부 자동으로
퍼집니다.** 일정이나 권한이 바뀔 때만 손이 가고 그런 일은 드뭅니다.

**결국 손으로 하는 것은 GPT 지침 재붙여넣기 하나뿐입니다** — ChatGPT Custom GPT의
Instructions는 API로 접근할 수 없어 어떤 방법으로도 자동화되지 않습니다. 그것만 ⚠️ 로
표시하고, 적용 알림 Issue 맨 위에 "이것만 직접 해 주세요"로 띄웁니다.

## 2026-08-03 — 누가 최신인지 한 번에 보기 (`check_fleet.py`)

자동 동기화를 붙여도 **동기화 자체는 각 저장소에 한 번 설치돼야** 돕니다(닭-달걀).
그래서 누가 설치했고 누가 뒤처졌는지 확인할 방법이 필요했습니다.

```bash
python3 scripts/check_fleet.py friend1/my-learning friend2/study
```

저장소마다 ① 자동 동기화 설치 여부 ② 엔진 파일이 템플릿과 같은지 ③ 마지막 동기화 시각을
한 줄로 보여 줍니다. 공개 API로 **읽기만** 합니다.

## ⚠️ 2026-08-03 — 지식 그래프가 단어 단위로 생기던 문제

토익 트랙에서 `procurement` · `adjacent` 같은 **단어 하나하나가 노드**로 잡혔습니다.
원인은 러너의 실수가 아니라 **개념의 단위가 어디에도 정의돼 있지 않았다**는 것입니다.
정의가 없으면 모델은 공부한 재료를 그대로 노드로 만듭니다. 노드가 수백 개가 되면
**막힌 길목이 묻혀** 그래프가 아무것도 말해 주지 않습니다.

층을 둘로 나눴습니다.

| | 무엇 | 어디 | 규모 |
|---|---|---|---|
| **개념** | 설명을 요구할 수 있는 것 (`Part5 시제 일치`) | 그래프 노드 | 10~30개로 수렴 |
| **항목** | 회상 대상 (`procurement = 조달`) | `drills.md` — 그래프 밖 | 수백 개여도 됨 |

항목은 버리지 않습니다. `drills.md`에 남아 복습에 쓰이고, 그래프에만 안 들어갑니다.

⚠️ **이건 머지만으로 끝나지 않습니다.** 러너 지침이 바뀌었으므로 ChatGPT → 내 GPT →
편집에서 `runner/instructions.md`의 회색 박스를 **다시 붙여넣어야** 합니다. 안 하면
이미 생긴 건 정리돼도 다음 세션부터 또 단어가 올라옵니다.

이미 그래프가 오염됐다면 `python3 scripts/migrate_drills.py` 로 무엇이 빠질지 먼저 보고,
맞으면 `--apply` 를 붙여 확정하세요. `mastery.md`는 건드리지 않습니다.
자세한 절차는 [`MIGRATION.md`](./MIGRATION.md).
