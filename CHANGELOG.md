# 바뀐 것들

> 템플릿이 고쳐지면 여기에 **무엇이·왜** 바뀌었는지 적습니다.
> `template-sync` 워크플로가 매주 월요일에 확인해 PR을 열고, 이 파일의 최근 항목을
> 그 PR 본문에 붙입니다 — 그래서 무엇을 받는지 모른 채 머지하지 않아도 됩니다.
>
> ⚠️ 표시는 **머지만으로 끝나지 않는 것**입니다. GPT 지침을 다시 붙여넣어야 합니다.

---

## 2026-08-03 — paper-scan이 주제를 넣어도 0편만 나오던 문제

`topics.yaml`에 주제를 넣었는데 인박스가 계속 "0편"이었다면 이 버그입니다. 원인이 셋이고,
셋 다 결과가 "0편"으로 같아서 겉으로는 구별할 수 없었습니다.

- **초록 없는 논문을 전부 버렸습니다.** OpenAlex는 출판사 라이선스 때문에 상당수 논문의
  초록을 주지 않습니다 — 특히 신간이 그렇습니다. API가 25편을 정상으로 돌려줘도 선별에서
  전멸했습니다. 이제 버리지 않고 뒤로만 밀고, 인박스에 "초록이 공개돼 있지 않다"고 적습니다.
- **조회 실패가 성공으로 처리됐습니다.** 모든 주제의 조회가 죽어도 종료 코드가 0이라,
  인박스엔 "0편"이 적히고 실패 알림도 뜨지 않았습니다. 이제 전부 실패하면 Issue가 옵니다.
- **OpenAlex 공용 대기열에서 막혔습니다.** `topics.yaml`에 `mailto:` 한 줄(선택)을 넣으면
  여유 있는 대기열로 갑니다. 일시적 오류는 3번까지 다시 시도합니다.

받은 뒤 **Actions → paper-scan → Run workflow** 로 바로 확인할 수 있습니다.

## 2026-08-03 — 템플릿 업데이트를 자동으로 받습니다 (`template-sync`)

지금까지는 템플릿이 고쳐질 때마다 "이 파일들을 복사하세요"를 사람이 전해야 했습니다.
한 번이면 몰라도 계속 생기면 아무도 하지 않게 되고, 결국 각자 다른 버전을 쓰게 됩니다.

이제 **매주 월요일에 워크플로가 확인해 PR을 엽니다.** 가져오는 것은 엔진
(`scripts/` · `runner/` · `presets/` · `templates/` · `.github/workflows/` · 문서)뿐이고,
`daily/` · `materials/` · `papers/` · `mastery.md` · `STATUS.md` · `drills.md` ·
`concepts.json` · `topics.yaml` · `README.md`는 **어느 경우에도 건드리지 않습니다.**

자동으로 합치지 않습니다 — PR을 열어 둘 뿐이라 무엇이 바뀌는지 보고 직접 누릅니다.

## ⚠️ 2026-08-03 — 지식 그래프가 단어 단위로 생기던 문제

토익 트랙에서 `procurement` · `adjacent` 같은 **단어 하나하나가 노드**로 잡혔습니다.
원인은 러너의 실수가 아니라 **개념의 단위가 어디에도 정의돼 있지 않았다**는 것입니다.
정의가 없으면 모델은 공부한 재료를 그대로 노드로 만듭니다. 노드가 수백 개가 되면
**막힌 길목이 묻혀** 그래프가 아무것도 말해 주지 않습니다.

층을 둘로 나눴습니다.

| | 무엇 | 어디 | 규모 |
|---|---|---|---|
| **개념** | 설명을 요구할 수 있는 것 (`Part5 시제 일치`) | 그래프 노드 | 10~30개로 수렴 |
| **항목** | 회상 대상 (`procurement = 조달`) | `drills.md` — 그래프 밖 | 수백 개여도 됨 |

항목은 버리지 않습니다. `drills.md`에 남아 복습에 쓰이고, 그래프에만 안 들어갑니다.

⚠️ **이건 머지만으로 끝나지 않습니다.** 러너 지침이 바뀌었으므로 ChatGPT → 내 GPT →
편집에서 `runner/instructions.md`의 회색 박스를 **다시 붙여넣어야** 합니다. 안 하면
이미 생긴 건 정리돼도 다음 세션부터 또 단어가 올라옵니다.

이미 그래프가 오염됐다면 `python3 scripts/migrate_drills.py` 로 무엇이 빠질지 먼저 보고,
맞으면 `--apply` 를 붙여 확정하세요. `mastery.md`는 건드리지 않습니다.
자세한 절차는 [`MIGRATION.md`](./MIGRATION.md).
