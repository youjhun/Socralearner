# 세팅 — 처음 한 번, 약 8분

> 이 문서만 따라 하면 **혼자 끝까지** 됩니다. 누구에게 물어볼 필요 없습니다.
> 막히면 맨 아래 [막히는 지점](#막히는-지점)을 보세요. 각 단계 끝에 **"이렇게 되면 성공"**을 적어 뒀습니다.

준비물: GitHub 계정 · ChatGPT 계정(커넥터 연결에 Plus 또는 Pro가 필요 — [대안](#chatgpt-plus가-없다면)도 있음)

> **토큰을 만들지 않습니다.** 예전 세팅은 PAT(개인 액세스 토큰)을 직접 발급해 붙여넣게 했습니다.
> 지금은 GitHub 승인 버튼 한 번이면 되고, 자격증명을 손으로 다루는 단계가 없습니다.
> 이미 예전 방식으로 쓰고 있다면 → **[`MIGRATION.md`](./MIGRATION.md)**

---

## 1단계 — 내 학습 저장소 만들기 (3분)

> 🚀 **더 쉬운 길이 있습니다.** [Topdown 시작하기](https://topdown-one-eta.vercel.app/setup)에서
> GitHub으로 로그인하면 **저장소를 대신 만들어 주고 2단계(Actions 권한)까지 함께 켭니다.**
> 그 길로 가면 아래 1~2단계를 건너뛰고 **3단계부터** 하면 됩니다.

직접 만들고 싶다면:

1. 이 repo(Socralearner) 페이지 우측 상단의 **`Use this template`** → **`Create a new repository`** 클릭.
   - 버튼이 안 보이면: **Fork**를 눌러도 됩니다.
2. 이름은 아무거나 (예: `my-learning`).
3. **Public / Private 고르기** — 나중에 바꿀 수 있으니 부담 갖지 마세요.

   | | 이럴 때 | 비고 |
   |---|---|---|
   | **Public** (권장) | 그래프 뷰어로 내 상태를 보고 싶다 · 나중에 **"이렇게 공부했습니다"로 보여주고** 싶다(연구실 컨택·포트폴리오) | 학습 기록이라 민감한 내용이 아닙니다 |
   | **Private** | 기록을 아무에게도 안 보이고 싶다 | 그래프 뷰어는 못 씁니다. **공부 자체는 똑같이 됩니다** |

> ✅ **성공**: 내 GitHub에 `STATUS.md`, `daily/`, `mastery.md`가 있는 repo가 생겼다.

---

## 2단계 — Actions 권한 켜기 (1분)

> **Topdown에서 저장소를 만들었다면 이 단계는 이미 끝나 있습니다.** 앱이 만들면서 함께 켭니다.
> `Use this template`으로 직접 만들었을 때만 아래를 하세요.

내 repo에서:

1. **Settings** → 왼쪽 메뉴 **Actions** → **General**
2. 맨 위 **Actions permissions**: `Allow all actions and reusable workflows` 선택 → **Save**
3. 아래로 스크롤 → **Workflow permissions**: **`Read and write permissions`** 선택 → **Save**

> ✅ **성공**: Workflow permissions가 "Read and write"로 저장됨.
> 세션 노트를 파일로 만드는 것은 AI가 아니라 **내 저장소 안에서 도는 자동화**입니다.
> 그래서 이 권한이 필요하고, 그래서 AI에게는 파일을 쓸 권한을 주지 않아도 됩니다.
>
> ⚠️ 빼먹으면 **세션은 저장되는데 파일이 안 생깁니다** — Issue는 열리고 CI도 도는데 마지막
> 커밋에서 조용히 실패합니다. 며칠 뒤 `daily/`가 비어 있는 것을 보고서야 알게 됩니다.

---

## 3단계 — Topdown Learning 앱 설치 (2분)

AI가 내 저장소를 읽고 기록을 남기려면 연결이 필요합니다. **토큰을 만들지 않습니다.**

1. **[Topdown Learning App 설치 페이지](https://github.com/apps/topdownlearning)** 를 엽니다.
   (열리지 않으면 관리자에게 설치 주소를 받으세요 — App 이름이 바뀌면 이 주소도 바뀝니다.)
2. **Install** → **Only select repositories** → **1단계에서 만든 저장소 하나만** 선택.

   > ⚠️ **꼭 하나만 고르세요.** 여러 개를 고르면 연결이 어느 저장소에 기록해야 할지 알 수 없어
   > 거부합니다. 엉뚱한 저장소에 세션이 들어가는 것보다 거부가 낫기 때문입니다.

3. 설치 화면에 뜨는 권한은 앱이 저장소 생성·템플릿 채우기까지 지원하기 때문에 조금 넓습니다.
   **학습 세션이 실제로 쓰는 것은 Contents 읽기와 Issues 읽기/쓰기 둘뿐**이고, 세션 노트를
   파일로 만드는 것은 앱이 아니라 내 저장소 안에서 도는 자동화입니다.

> ✅ **성공**: 내 repo → **Settings → GitHub Apps**에 Topdown Learning이 보인다.

### 이 연결, 위험하지 않나요?

| 할 수 있는 일 | 할 수 **없는** 일 |
|---|---|
| 위에서 **선택한 저장소 하나**의 파일을 읽기 | 다른 저장소는 이름조차 볼 수 없음 |
| 그 저장소에 Issue를 열고 코멘트 달기 | 저장소 삭제·공개설정 변경·다른 사람 초대 |
| (앱 쪽 기능) 저장소를 만들어 템플릿을 채워 넣기 | 내 GitHub 계정 설정 건드리기 |

> **학습 세션(커넥터)이 쓰는 권한은 읽기와 Issue뿐입니다.** 세션 중에 코드가 고쳐지는 일은
> 없습니다 — 파일을 만드는 것은 내 저장소의 자동화이고, 그 결과는 커밋 이력에 남습니다.

연결은 언제든 **Settings → Applications → Topdown Learning → Revoke**로 끊을 수 있습니다.
끊으면 즉시 아무것도 읽지 못합니다.

---

## 4단계 — ChatGPT에 연결하기 (2분)

1. ChatGPT → **설정(Settings)** → **커넥터(Connectors)** → **고급(Advanced)** → **개발자 모드(Developer mode)** 를 켭니다.
2. 커넥터 화면에서 **추가(Add)** → 서버 주소에 아래를 넣습니다:

   ```
   https://topdown-one-eta.vercel.app/api/mcp
   ```

   > 프리뷰 배포 주소(`topdown-...-hash.vercel.app`)를 넣지 마세요. 그 배포가 사라지면 연결이 끊깁니다.

3. **연결(Connect)** 을 누르면 GitHub 승인 창이 뜹니다 → **승인**.
4. 끝입니다. 붙여넣을 것도, 저장소 이름을 적을 곳도 없습니다 — 저장소는 3단계의 설치에서 옵니다.

> ✅ **성공**: 커넥터 목록에 연결이 보이고, 도구 4개(`get_state` · `read_doc` · `get_paper` · `save_session`)가 보인다.

> **왜 도구가 4개뿐인가**: 예전에는 읽기 동작만 8개였습니다. AI가 그중 무엇을 부를지 고르다
> 틀리면 오류가 났고, 오류가 반복되면 AI는 도구를 포기하고 **웹 검색으로 새거나** "읽기에
> 실패했습니다, 상태를 붙여넣어 주세요"라고 했습니다. 고를 것이 없으면 틀릴 수도 없습니다.

---

## 5단계 — 첫 세션 (5분)

ChatGPT 새 대화에서:

```
오늘 세션 시작.
```

그러면 AI가:
1. `get_state`로 내 상태를 **알아서** 읽고 (아직 비어 있으면 **목표를 묻습니다**)
2. 목표를 말하면 → 학습 경로를 **가설로** 제안 → 승인하면 시작
3. **"먼저 네 말로 설명해봐"** 라고 합니다 → 모르면 모르는 대로, 아는 만큼 답하세요. **이게 핵심입니다.**
4. **기록은 대화하는 동안 알아서 쌓입니다.** 주제가 바뀔 때만 *"여기까지 기록해둘까요?"*
   라고 묻고, **"기록해줘"** 라고 답하면 마무리한 뒤 바로 다음 주제로 넘어갑니다.

**목표 예시** (아무 주제나 됩니다):
- "반도체 소자 연구실 인턴 준비 — MOSFET 동작을 스스로 설명할 수 있게"
- "이 논문(링크)을 이해하고 재현하고 싶다"
- "선형대수를 논문 읽을 수준으로"

> ✅ **성공 확인 (중요)**: 1~2분 뒤 **`daily/` 폴더에 오늘 날짜 파일**이 생깁니다.
> 가장 쉬운 확인은 [Topdown 워크스페이스](https://topdown-one-eta.vercel.app/setup)를 열어
> **연속 학습 일수 배지가 1일**로 뜨는지 보는 것입니다 — GitHub에 들어갈 필요가 없습니다.
> (직접 보고 싶다면 repo → Actions 탭, 그리고 그 Issue에 달리는 자동 코멘트.)

---

## 6단계 (선택) — 논문 자동 수집 켜기 (2분)

연구 주제를 따라가는 중이라면, **매주 새 논문**을 자동으로 모아 줄 수 있습니다.

> 🗣 **손으로 안 적어도 됩니다.** 세션에서 *"연구 주제 잡아줘"* 라고 하면 러너가 목표와
> 읽은 논문을 보고 주제를 잡아 `topics.yaml`까지 대신 씁니다(Topdown 논문 탭의
> 「연구 주제 잡기」 버튼도 같은 일을 합니다). 아래는 직접 적고 싶을 때만 보세요.

1. repo의 **`topics.yaml`** 을 편집합니다(GitHub 웹에서 연필 아이콘).
2. 예시를 지우고 내 주제를 **영어 query**로 적습니다:
   ```yaml
   topics:
     - id: my-lab
       label: "우리 연구실 주제"
       query: "neural prosthetics implantable electrode decoding"
   ```
3. 커밋하면 끝. 매주 월요일 아침에 돌면서 새 논문이 있으면 **Issue로 알려주고** `papers/inbox.md`에 모읍니다.

세션에서 *"이번 주 새 논문 같이 보자"* 라고 하면 러너가 인박스에서 골라 줍니다.

> ✅ **성공**: Actions에서 paper-scan이 초록불이고, `papers/inbox.md`가 갱신된다.

---

## 7단계 (선택) — Topdown 워크스페이스 열기

세션을 몇 번 쌓으면 **Topdown 앱**에서 내 저장소를 그대로 열 수 있습니다:

```
https://topdown-one-eta.vercel.app/workspace/repo/내아이디/my-learning
```

| 탭 | 무엇이 보이나 | 어디서 오나 |
|---|---|---|
| **논문** | 내가 올린 원문 자료 · 이번 주 새 논문 · 막힌 개념을 뚫을 자료 | `materials/` · `papers/inbox.md` |
| **학습** | 오늘 할 것 · 주제별 약점·복습 · 미뤄 둔 것 · 연속 학습 일수 | `STATUS.md` · `concepts.json` |
| **지식 그래프** | 내가 설명할 수 있게 된 개념과 막힌 길목. 노드를 감추거나 이름을 합칠 수도 있습니다 | `concepts.json` |

연구·산출물은 「부가 기능」 메뉴에 있습니다.

---

## 🗣 세션 시작 문구 모음 (복사해서 쓰세요)

| 상황 | 시작 문구 |
|---|---|
| 매일 학습 (기본) | `오늘 세션 시작.` |
| 논문 한 편 깊이 읽기 | (논문 PDF를 첨부하고) `이 논문 정독하자 — 절 단위로.` |
| 실라버스·강의노트 넣기 | (PDF를 첨부하고) `이 자료 증류해줘 — 요약·개념 지도·빈칸 문제로.` |
| 들은 과목 복원 시작 | `[과목이름] 복원 스캔부터 하자 — 기억나는 대로 설명해볼게.` |
| 이번 주 새 논문 보기 | `이번 주 논문 같이 골라보자.` |
| 막힌 개념 집중 | `막힌 길목부터 하자.` |
| 세션 마무리 | `기록해줘.` (러너가 먼저 물어봅니다) |

> 예전 세팅에서 필요했던 **`STATUS.md 읽고`** 같은 말머리는 이제 필요 없습니다.
> 그냥 "오늘 세션 시작"이면 AI가 알아서 불러옵니다.

> ⚠️ **GitHub 커넥터를 함께 켜 두었다면 주의.** 러너가 묻기 전에 먼저 "저장해줘"라고 하면
> ChatGPT가 GitHub 쪽으로 새서 **형식이 다른 Issue**를 만들 수 있습니다. 그 Issue는
> 자동화가 읽지 못해 노트 파일이 만들어지지 않습니다 — 저장된 것처럼 보이지만 기록은
> 없습니다. 그럴 때는 **`save_session으로 기록해줘`** 라고 도구 이름을 붙이세요.

---

## 다음부터는

**"오늘 세션 시작"** 한 마디면 됩니다.
매일 쓰는 법과 기록 읽는 법은 **[`GUIDE.md`](./GUIDE.md)**, 왜 이렇게 공부하는지는 **[`METHOD.md`](./METHOD.md)**.

---

## 막히는 지점

**Q. `Use this template` 버튼이 없어요.**
Fork를 쓰세요. 똑같이 동작합니다.

**Q. Issue는 생겼는데 `daily/`에 파일이 안 생겨요.** ← 가장 흔한 문제
① **2단계(Workflow permissions = Read and write)** 를 했는지 확인하세요. 대부분 이것 때문입니다.
② Issue 제목이 `[학습]`으로 시작하는지 확인 (자동화가 이걸로 걸러냅니다).
③ 그래도 안 되면 그 Issue에 `/기록` 이라고 코멘트를 달면 다시 시도합니다.
④ repo의 **Actions 탭 → 실패한 실행 → 로그**를 보면 이유가 한국어로 나옵니다.

**Q. 연결할 때 "학습 저장소를 한 개로 특정하지 못했다"고 나옵니다.**
3단계에서 GitHub App을 **저장소 여러 개**에 설치한 것입니다.
Settings → Applications → Topdown Learning → **Configure** → **Only select repositories** 에서
학습 저장소 **하나만** 남기고 다시 연결하세요.

**Q. AI가 상태를 안 읽고 그냥 대답해요. / "상태를 붙여넣어 주세요"라고 합니다.**
① 커넥터 목록에 **연결됨**으로 보이는지 확인하세요.
② 대화에서 커넥터가 **켜져 있는지** 확인하세요 — 입력칸 왼쪽 **+** → **Topdown** 을 고르면
   입력칸에 `Topdown` 칩이 뜹니다. **이 칩이 없으면 도구 없이 대답합니다.**
   연결은 한 번이지만 **커넥터 선택은 새 대화마다** 해야 합니다.
③ 그래도 그러면 `get_state 불러줘`라고 한 번만 말해 보세요. 이후로는 알아서 부릅니다.

> 이건 예전 세팅의 고질병이었고, 도구를 4개로 줄이고 인자 없이 부를 수 있게 만든 이유입니다.
> 그래도 안 되면 **버그로 신고해 주세요** — 지침으로 덮을 문제가 아니라 저희가 고칠 문제입니다.

**Q. 기록이 3줄로 짧게만 남아요.**
**이제 서버가 거부합니다.** 너무 짧은 노트는 저장되지 않고 AI에게 "줄이지 말고 전부 보내라"는
응답이 돌아갑니다. 그런데도 짧게 남는다면 버그이니 신고해 주세요.

**Q. 기록이 실패했는데 AI가 만든 내용은 살리고 싶습니다.**
AI가 출력한 본문을 그대로 복사해 내 repo → **Issues → New issue** 에 붙여넣으세요.
**제목만 규약을 지키면 됩니다** — `[학습] YYYY-MM-DD <slug> — <한 줄 제목>`.
자동화는 사람이 연 Issue와 AI가 연 Issue를 구별하지 않습니다.

**Q. AI가 그냥 답을 알려줘요.**
"내가 먼저 설명할게, 답 먼저 주지 마"라고 하세요.

### ChatGPT Plus가 없다면

커넥터(개발자 모드)는 Plus/Pro가 필요합니다. 무료 계정이라면:

- **무료 대안**: 일반 ChatGPT 대화에 [`runner/instructions.md`](./runner/instructions.md)의 회색 박스를
  붙여넣고 공부한 뒤, 세션 끝에 AI가 출력한 노트를 복사해 **내 repo에 직접 Issue로 붙여넣기**
  (제목은 `[학습] YYYY-MM-DD slug — 제목`). 자동화는 똑같이 돌아 파일을 만들어 줍니다.
  상태 불러오기는 수동이 되지만, **기록과 파일 생성은 완전히 동일**합니다.

> 앞으로 Socralearner를 ChatGPT 앱으로 심사 제출하면 **무료 계정에서도** 커넥터 없이 쓸 수 있게 됩니다.
> 지금은 그 전 단계입니다.
